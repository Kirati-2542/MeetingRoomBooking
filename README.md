# EduMeet - Room Booking System

เว็บแอปพลิเคชันจองห้องประชุมออนไลน์ ใช้งานง่าย สวยงาม ทันสมัย ตอบโจทย์องค์กรและสถานศึกษา

## 🚀 ฟีเจอร์หลัก (Features)

-   **ระบบจองห้องประชุม**: เลือกวันเวลาจองห้อง ดูสถานะห้องว่างแบบ Real-time
-   **ระบบอนุมัติ**: สำหรับผู้อนุมัติ (Approver) ในการตรวจสอบและอนุมัติการจอง
-   **แดชบอร์ด**: แสดงสถิติการใช้งานห้องประชุม กราฟสวยงาม ข้อมูลครบถ้วน
-   **จัดการห้องประชุม**: เพิ่ม ลบ แก้ไข ข้อมูลห้องประชุมพร้อมรูปภาพ
-   **แจ้งเตือนทางอีเมล**: แจ้งเตือนเมื่อมีการจองใหม่ และแจ้งผลการอนุมัติ (ใช้ Resend)
-   **UI สวยงาม**: ธีมสีฟ้าพาสเทล (Pastel Blue) ทันสมัย สบายตา รองรับการใช้งานบนมือถือ (Responsive)

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

-   **Frontend**: React (Vite), TypeScript, Tailwind CSS
-   **Backend / Database**: Supabase (PostgreSQL)
-   **Email Service**: Resend (via Supabase Edge Functions)
-   **Icons**: Lucide React
-   **Charts**: Recharts

## 📥 การติดตั้งและรันโปรเจกต์ (Installation)

1.  **Clone โปรเจกต์**
    ```bash
    git clone https://github.com/your-username/edumeet-booking.git
    cd edumeet-booking
    ```

2.  **ติดตั้ง dependencies**
    ```bash
    npm install
    ```

3.  **ตั้งค่า Environment Variables**
    สร้างไฟล์ `.env` ที่ root folder และใส่ค่า Config ของ Supabase:
    ```env
    VITE_SUPABASE_URL=your_supabase_url
    VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
    ```

4.  **ตั้งค่า Database**
    -   นำไฟล์ `supabase_schema.sql` ไปรันใน SQL Editor ของ Supabase เพื่อสร้างตารางและข้อมูลเริ่มต้น

5.  **ตั้งค่า Edge Function (สำหรับอีเมล)**
    -   สร้าง Function `send-notification-email` ใน Supabase
    -   ตั้งค่า Secret `RESEND_API_KEY` และ `SITE_URL`

6.  **รันโปรเจกต์**
    ```bash
    npm run dev
    ```

## 🔐 ข้อมูลเข้าสู่ระบบ (Demo Accounts)

| Role | Username | Password |
| :--- | :--- | :--- |
| **Admin** | `admin` | `admin123` |
| **Approver** | `approver` | `approver123` |
| **User** | `user` | `user123` |

## 🛡 Security Note

โปรเจกต์นี้ใช้ **Custom Authentication** (ตาราง `users` แยกเอง) ไม่ได้ใช้ Supabase Auth (GoTrue) ดังนั้น RLS Policy ใน Database ถูกตั้งค่าไว้แบบเปิดเพื่อให้ Function ใช้งานได้ หากจะนำไปใช้จริงควรปรับปรุงระบบ Authentication ให้ปลอดภัยยิ่งขึ้น

---
Developed by EduMeet Team
